kw1.
SELECT plik, nr_klienta, liczba_odbitek
FROM zamowienia
WHERE liczba_odbitek > 100 AND rodzaj = 'matowy'

kw2.
SELECT (liczba_odbitek * cena) AS 'Do zapłaty'
FROM zamowienia JOIN zdjecia ON zdjecia.rodzaj = zamowienia.rodzaj
WHERE nr_klienta = 3

kw3.
SELECT SUM(liczba_odbitek)
FROM zamowienia
WHERE rodzaj = 'błyszczący'

kw4.
CREATE USER 'Anna' @'localhost' IDENTIFIED BY '@NNa'